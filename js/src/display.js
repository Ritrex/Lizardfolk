var cnv = document.getElementById("cnv");
var ctx = cnv.getContext("2d");
var tileXsize = cnv.width / 40;
var tileYsize = cnv.height / 40;

function setDisplay() {
  for (var i = 0; i < map.layout.length; i++) {
    for (var j = 0; map.layout[0].length; j++) {}
  }
}
function updateDisplay() {
  floor1.forEach(line => {
    line.forEach(tile => {});
  });
}
